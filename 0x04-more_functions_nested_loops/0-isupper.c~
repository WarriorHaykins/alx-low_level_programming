#include "main.h"
/**
*_isupper - checks for an upper case character and print 1 if uppercase and 0 if otherwise
*c: the character to be checked
*Return: 1 if uppercase and 0 if otherwise
*/
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
