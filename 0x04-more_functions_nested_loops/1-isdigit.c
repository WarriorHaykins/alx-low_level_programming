#include "main.h"
/**
*_isdigit - checks for a digit from 0 to 9,print 1 if digit and 0 if otherwise
*@c: the int to be checked
*Return: print 1 if c is digit and 0 otherwise
*/
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
