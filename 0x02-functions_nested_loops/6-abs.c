/**
*_abs - checks the absolute value and prints it
*Return: returns the absolute value of the integer
*@r: the integer to be checked
*/

int _abs(int r)
{
if (r >= 0 || r <= 0)
{
return (r);
}
return (0);
}
